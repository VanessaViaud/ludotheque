spring:
  application:
    name: ludotheque
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
  datasource:
    url: jdbc:postgresql://localhost/LUDO_DB
    username: postgres
    password: Pa$$w0rd
